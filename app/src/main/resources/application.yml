---
spring:
  profiles:
    default: development
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update

#springdoc:
#  api-docs:
#    path: /api-docs
#  swagger-ui:
#    path: /swagger
#    enabled: true

rsa:
  private-key: classpath:certs/private.pem
  public-key: classpath:certs/public.pem

server:
  address: 0.0.0.0
  port: 7070