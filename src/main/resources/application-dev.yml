---
spring:
  jpa:
    show-sql: true
  output:
    ansi:
      enabled: always

app:
  user:
    email: hexlet@example.com
    password: qwerty

logging:
  level:
    root: WARN
    org:
      springframework:
        web: DEBUG
        security: DEBUG

